import noop from 'src/utils/noop';

export default {
    children: 'Sample Checkbox',
    className: 'demo-checkbox',
    checked: true,
    readOnly: false,
    disabled: false,
    onChange: noop
};
