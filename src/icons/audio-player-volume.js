import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const AudioPlayerVolume = () =>
    <Icon id={ ICON_IDS.AUDIO_PLAYER_VOLUME } className="Icons-audio-player-volume" />;

export default AudioPlayerVolume;
