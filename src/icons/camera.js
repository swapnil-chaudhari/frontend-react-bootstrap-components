import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const Camera = () => <Icon className="Icons-camera" id={ ICON_IDS.CAMERA } />;

export default Camera;
