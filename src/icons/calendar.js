import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const Calendar = () =>
    <Icon id={ ICON_IDS.CALENDAR } className="Icons-calendar" />;

export default Calendar;
