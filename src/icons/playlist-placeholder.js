import React from 'react';
import { Icon } from 'src';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const PlaylistPlaceholder = () => (
    <Icon className="Icons-playlist-placeholder" id={ ICON_IDS.PLAYLIST_PLACEHOLDER } />
);

export default PlaylistPlaceholder;
