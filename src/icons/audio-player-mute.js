import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const AudioPlayerMute = () =>
    <Icon id={ ICON_IDS.AUDIO_PLAYER_MUTE } className="Icons-audio-player-mute" />;

export default AudioPlayerMute;
