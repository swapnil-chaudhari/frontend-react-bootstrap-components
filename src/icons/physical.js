import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const Physical = () => <Icon className="Icons-physical" id={ ICON_IDS.PHYSICAL } />;

export default Physical;
