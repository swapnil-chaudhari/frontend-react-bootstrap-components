import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const AudioPlayerNext = () =>
    <Icon id={ ICON_IDS.AUDIO_PLAYER_NEXT } className="Icons-audio-player-next" />;

export default AudioPlayerNext;
