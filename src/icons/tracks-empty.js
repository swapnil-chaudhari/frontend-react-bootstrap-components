import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const TracksEmpty = () =>
    <Icon id={ ICON_IDS.TRACKS_EMPTY } className="Icons-tracks-empty" />;

export default TracksEmpty;
