import React from 'react';
import { Icon } from 'src/index';
import { ICON_IDS } from 'src/svg-definitions/svgs';

const Chevron = () =>
    <Icon id={ ICON_IDS.CHEVRON } className="Icons-chevron" />;

export default Chevron;
