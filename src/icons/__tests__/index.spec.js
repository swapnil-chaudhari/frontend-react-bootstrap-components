import React from 'react';
import renderShallow from 'render-shallow';
import {
    Aan,
    ArtworkEmpty,
    Attention,
    AudioDisabled,
    AudioEnabled,
    AudioPlayerMute,
    AudioPlayerNext,
    AudioPlayerNextLite,
    AudioPlayerPause,
    AudioPlayerPlay,
    AudioPlayerPrevious,
    AudioPlayerPreviousLite,
    AudioPlayerVolume,
    AudioPlayerPauseLite,
    Calendar,
    CompleteCheckmark,
    IncompleteAlert,
    Camera,
    Chevron,
    CleanVersion,
    Info,
    Close,
    Digital,
    DigitalAudio,
    Explicit,
    Film,
    LoadingRing,
    Physical,
    PhysicalAudio,
    RightDirectionArrow,
    StatusComplete,
    StatusInProgress,
    StatusRed,
    StatusSubmitted,
    SuccessCheckmark,
    TrashCan,
    Upload,
    ThreeRightDirectedArrows,
    LeftPaginationArrow,
    RightPaginationArrow,
    Logo,
    LoadingArrows,
    Edit,
    ExternalLink,
    UpAndDownArrows,
    DownloadBox,
    Play,
    Stop,
    TracksEmpty,
    TracksCompleteCheckmark,
    ViewMoreNextArrow,
    Plus,
    DownDirectionArrow,
    UpDirectionArrow,
    ThreeDots,
    Refresh,
    ServerError,
    NewProduct,
    GoToCatalog,
    Advertise,
    FilledChevronDown,
    FilledChevronUp,
    TransparentLogo,
    History,
    Pdf,
    Xls,
    RoyaltyShareLogo,
    Error404,
    LogoAndName,
    Maintenance,
    Gear,
    Spotify,
    AppleMusic,
    Twitter,
    Instagram,
    Facebook,
    YouTube,
    PlaylistPlaceholder,
    UploadArrow,
    ImportArrow,
    Video
} from '../';

describe('icons', () => {

    describe('<ArtworkEmpty>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ArtworkEmpty />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Attention>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Attention />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioDisabled>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioDisabled />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioEnabled>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioEnabled />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerMute>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerMute />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerNext>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerNext />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerNextLite>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerNextLite />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerPause>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerPause />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerPauseLite>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerPauseLite />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerPlay>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerPlay />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerPrevious>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerPrevious />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerPreviousLite>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerPreviousLite />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AudioPlayerVolume>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AudioPlayerVolume />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<CompleteCheckmark>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<CompleteCheckmark />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<IncompleteAlert>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<IncompleteAlert />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<CleanVersion>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<CleanVersion />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Calendar>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Calendar />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Chevron>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Chevron />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Info>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Info />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<Digital>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Digital />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<DigitalAudio>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<DigitalAudio />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<Explicit>', () => {
        describe('when it renders with required props', () => {
            let component;

            beforeAll(() => {
                component = renderShallow(<Explicit />).output;
            });

            test('renders an <Icon> with the correct prop.id', () => {
                expect(component).toMatchSnapshot();
            });
        });

        describe('when it renders with a style class', () => {
            const props = { className: 'test-class' };
            let component;

            beforeAll(() => {
                component = renderShallow(<Explicit { ...props } />).output;
            });

            test('renders an <Icon> the supplied style class', () => {
                expect(component).toMatchSnapshot();
            });
        });
    });

    describe('<Film>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Film />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<Physical>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Physical />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<PhysicalAudio>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<PhysicalAudio />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<StatusInProgress>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<StatusInProgress />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<StatusRed>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<StatusRed />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<StatusSubmitted>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<StatusSubmitted />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<RightDirectionArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<RightDirectionArrow />).output;
        });

        test('renders an <Icon> with correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<StatusComplete>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<StatusComplete />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<Close>', () => {

        describe('when it renders with no props', () => {
            let component;

            beforeAll(() => {
                component = renderShallow(<Close />).output;
            });

            test('renders an <Icon> with the correct prop.id', () => {
                expect(component).toMatchSnapshot();
            });
        });

        describe('when it renders with a className prop', () => {
            const props = {
                className: 'test-class-name'
            };
            let component;

            beforeAll(() => {
                component = renderShallow(<Close { ...props } />).output;
            });

            test('renders an <Icon> with the className prop', () => {
                expect(component).toMatchSnapshot();
            });
        });

        describe('when it renders with an onClick prop', () => {
            const props = {
                onClick: () => {}
            };
            let component;

            beforeAll(() => {
                component = renderShallow(<Close { ...props } />).output;
            });

            test('renders an <Icon> and passes the onClick prop to it', () => {
                expect(component.props.onClick).toEqual(props.onClick);
            });
        });

    });

    describe('<Upload>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Upload />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<LoadingRing>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<LoadingRing />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });

    });

    describe('<SuccessCheckmark>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<SuccessCheckmark />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<TrashCan>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<TrashCan />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<ThreeRightDirectedArrows>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ThreeRightDirectedArrows />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<LeftPaginationArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<LeftPaginationArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<RightPaginationArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<RightPaginationArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Logo>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Logo />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<LoadingArrows>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<LoadingArrows />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Edit>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Edit />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<ExternalLink>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ExternalLink />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });
    describe('<UpAndDownArrows>', () => {
        let component;
        const props = { className: 'test-class' };

        beforeAll(() => {
            component = renderShallow(<UpAndDownArrows { ...props } />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<DownloadBox />', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<DownloadBox />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Play />', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Play />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Stop />', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Stop />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<TracksEmpty>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<TracksEmpty />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<TracksCompleteCheckmark>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<TracksCompleteCheckmark />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<ViewMoreNextArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ViewMoreNextArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Plus>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Plus />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<DownDirectionArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<DownDirectionArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });


    describe('<UpDirectionArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<UpDirectionArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<ThreeDots>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ThreeDots />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Refresh>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Refresh />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<ServerError>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ServerError />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<NewProduct>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<NewProduct />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<GoToCatalog>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<GoToCatalog />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Advertise>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Advertise />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<FilledChevronDown>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<FilledChevronDown />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<FilledChevronUp>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<FilledChevronUp />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<TransparentLogo>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<TransparentLogo />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<History>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<History />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Pdf>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Pdf />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Xls>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Xls />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<RoyaltyShareLogo>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<RoyaltyShareLogo />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Error404>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Error404 />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<LogoAndName>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<LogoAndName />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Gear>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Gear />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Maintenance>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Maintenance />).output;
        });

        test('renders an SVG with the correct prop.id and tags', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Spotify>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Spotify />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AppleMusic>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<AppleMusic />).output;
        });

        test('renders an SVG with the correct prop.id and tags', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Instagram />', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Instagram />).output;
        });

        test('renders an SVG with the correct prop.id and tags', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Twitter>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Twitter />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Facebook>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Facebook />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<YouTube>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<YouTube />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<AAN>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Aan />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Camera>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Camera />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<PlaylistPlaceholder>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<PlaylistPlaceholder />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<UploadArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<UploadArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<ImportArrow>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<ImportArrow />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });

    describe('<Video>', () => {
        let component;

        beforeAll(() => {
            component = renderShallow(<Video />).output;
        });

        test('renders an <Icon> with the correct prop.id', () => {
            expect(component).toMatchSnapshot();
        });
    });
});
